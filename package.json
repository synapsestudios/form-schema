{
  "name": "@synapsestudios/form-schema",
  "version": "1.5.3",
  "description": "A package to help create and export form schema for building dynamic forms",
  "main": "lib/index.js",
  "repository": "https://github.com/synapsestudios/form-schema",
  "author": "Synapse Studios, LLC",
  "license": "MIT",
  "scripts": {
    "lint": "eslint lib/",
    "test": "jest",
    "build": "babel src -d lib",
    "prepublish": "npm test && npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-jest": "^19.0.0",
    "babel-preset-env": "^1.3.2",
    "eslint": "^3.18.0",
    "jest": "^19.0.2"
  },
  "dependencies": {
    "immutable": "^3.8.1"
  }
}
